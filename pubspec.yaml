name: ludo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.9.3
  provider: ^6.1.5+1

dev_dependencies:
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/icon/
    - assets/avatars/
    - assets/images/

# 👇 This part must be OUTSIDE the "flutter:" block
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/ludo.png"
  adaptive_icon_background: "#00080F"
  adaptive_icon_foreground: "assets/icon/ludo.png"
